# Function: useRuntime()

```ts
function useRuntime(): ReactRuntime;
```

Access the React runtime (snapshots, head application, error rendering).

## Returns

[`ReactRuntime`](../../ReactRuntime/classes/ReactRuntime.md)

The React runtime.
