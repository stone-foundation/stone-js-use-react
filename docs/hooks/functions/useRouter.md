# Function: useRouter()

```ts
function useRouter(): IRouter;
```

Access the router (navigate, generate URLs, inspect the current route).

## Returns

[`IRouter`](../../declarations/type-aliases/IRouter.md)

The router.
