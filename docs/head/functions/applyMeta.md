# Function: applyMeta()

```ts
function applyMeta(document, meta): void;
```

Apply (create or update) a `<meta>` tag in the document head.

## Parameters

### document

`Document`

The target document.

### meta

`MetaDescriptor`

The meta descriptor.

## Returns

`void`
