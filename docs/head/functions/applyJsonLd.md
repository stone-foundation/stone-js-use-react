# Function: applyJsonLd()

```ts
function applyJsonLd(document, data): void;
```

Append a JSON-LD structured-data block to the document head.

## Parameters

### document

`Document`

The target document.

### data

`Record`\<`string`, `unknown`\>

The JSON-LD object.

## Returns

`void`
