# Function: applyLink()

```ts
function applyLink(document, link): void;
```

Apply (create or update) a `<link>` tag in the document head.

## Parameters

### document

`Document`

The target document.

### link

`LinkDescriptor`

The link descriptor.

## Returns

`void`
