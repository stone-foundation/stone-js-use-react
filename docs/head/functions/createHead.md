# Function: createHead()

```ts
function createHead(initial?): HeadManager;
```

Create a new [HeadManager](../classes/HeadManager.md).

## Parameters

### initial?

`HeadContext`

An optional initial head context.

## Returns

[`HeadManager`](../classes/HeadManager.md)

A new HeadManager.
