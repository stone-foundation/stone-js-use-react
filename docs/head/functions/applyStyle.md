# Function: applyStyle()

```ts
function applyStyle(document, style): void;
```

Apply an inline `<style>` tag in the document head (deduplicated by content).

## Parameters

### document

`Document`

The target document.

### style

`StyleDescriptor`

The style descriptor.

## Returns

`void`
