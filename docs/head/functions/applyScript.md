# Function: applyScript()

```ts
function applyScript(document, script): void;
```

Apply (create or update) a `<script>` tag in the document head.

## Parameters

### document

`Document`

The target document.

### script

`ScriptDescriptor`

The script descriptor.

## Returns

`void`
